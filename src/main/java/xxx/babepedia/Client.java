package xxx.babepedia;

import java.io.IOException;
import java.net.URI;
import wip.HTTP;

final class Client extends xxx.Client
	{
	/*
	@Override
	public String get(final URI uri) throws IOException
		{
		return execute(getRequest(uri), HTTP.ISO_8859_1_TO_UTF_8);
		}
	*/
	}
