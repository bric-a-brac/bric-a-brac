package sex.sex4u;

public final class Sex4U
	{
	/*
	@Override
	public Iterable<Escort> get() throws IOException
		{
		final var escorts = new ArrayList<Escort>();

		for (final var url : parseListings())
			{
			System.out.println(url);
			//escorts.add(parseEscort(url));
			}

		return escorts;
		}
	*/

	/*
	private List<String> parseListings() throws IOException
		{
		final var links = new ArrayList<String>();

		var url = "https://sex4u.ch/fr/suisse/escorts";

		while (url != null)
			{
			final var document = getAsDocument(url);

			links.addAll(Parser.links(document));

			url = null;
			}

		return links;
		}
	*/

	/*
	private Escort parseEscort(final String url) throws IOException
		{
		return new Escort();
		}
	*/

	public static void main(String[] args) throws Throwable
		{
		}
	}
