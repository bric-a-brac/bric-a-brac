package website.builder.actions;

import java.io.IOException;
import java.nio.file.Path;

public class CopyFile implements IOperation<Path, IOException>
	{
	@Override
	public void accept(final Path path) throws IOException
		{
		}
	}
