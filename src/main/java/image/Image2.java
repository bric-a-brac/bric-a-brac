package image;

public class Image2
	{
	/**
	 * @throws NullArgumentException
	 * 
	 * @since 0.1.0
	 */
	/*
	public final <T> List<T> pixelize(final Function<Pixel, T> pixelizer)
		{
		notNull(pixelizer);

		return getPixels().stream().map(pixelizer).collect(Collectors.toList());
		}
	*/
	}
