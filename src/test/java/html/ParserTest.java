package html;

import java.io.IOException;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

public class ParserTest extends Assertions
	{
	@Test
	public void testParse() throws IOException
		{
		//final var html = Files.readString(Paths.get("tests/html/document.html"));

		//final var document = new Parser().parse(html);

		//assertNotNull(new Parser().parse(html));

		//assertNotNull(Parser.parse(html, ""));
		}
	}
